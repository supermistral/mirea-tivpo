note
    description : "Base Pet class"
    date        : "$Date$"
    revision    : "$Revision$"

deferred class PET

feature

    get_age: REAL
        deferred
    end

    get_name: STRING
        deferred
    end

end
